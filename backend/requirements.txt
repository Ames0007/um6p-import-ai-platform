# --- Framework web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0        # gestionnaire de process en production (workers uvicorn)
python-multipart==0.0.20

# --- Base de données ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Cache / files d'attente ---
redis==5.2.1
rq==2.0.0

# --- Ingestion documentaire (Phase 2) ---
pypdf==5.1.0            # extraction de texte + comptage de pages PDF
python-docx==1.1.2      # extraction DOCX
openpyxl==3.1.5         # extraction XLSX
# OCR : intégré via des "hooks" optionnels (voir app/ingestion/ocr).
# Pour activer l'OCR, installer en plus : pytesseract, pdf2image, Pillow (+ tesseract-ocr système).

# --- Rapports de conformité (Phase 5) ---
reportlab==4.2.5        # export PDF (repli HTML si absent)

# --- Configuration & validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Sécurité / JWT (préparation SSO) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- IA (Claude) ---
anthropic==0.42.0

# --- Outils ---
python-dotenv==1.0.1
